# Source Code

This directory will contain the actual Omni10-3DS source code.

## Planned Structure

```
source/
├── arm9/           # ARM9 code (file system, crypto, low-level)
├── arm11/          # ARM11 code (UI, networking, higher level)
├── common/         # Shared code
├── lua/            # Lua interpreter + bindings
├── o10/            # .o10 script parser & runtime
└── main.c          # Entry point
```

## Status

Currently empty – waiting for the first real code commits.
