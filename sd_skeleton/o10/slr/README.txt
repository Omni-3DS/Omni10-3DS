Omni10 o10script sources (sdmc:/o10/slr/)

1. Create name.slr on PC or on SD
2. On 3DS: Omni10 → HOME Scripts → select → compile on console
3. Runs as opaque name.o10 (only Omni10 understands it)

Example lines:
  print "Hello"
  wait 400
  info
